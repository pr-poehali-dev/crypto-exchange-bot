# 🚀 Инструкция по запуску Crypto Exchange Bot

## 📋 Оглавление
1. [Получение API токена Crypto Bot](#1-получение-api-токена-crypto-bot)
2. [Настройка секретов в проекте](#2-настройка-секретов-в-проекте)
3. [Настройка Webhook](#3-настройка-webhook)
4. [Создание Telegram бота](#4-создание-telegram-бота)
5. [Подключение Web App к боту](#5-подключение-web-app-к-боту)
6. [Тестирование](#6-тестирование)
7. [Частые проблемы](#7-частые-проблемы)

---

## 1. Получение API токена Crypto Bot

### Шаг 1.1: Создание приложения в Crypto Bot

1. Откройте Telegram и найдите бота **[@CryptoBot](https://t.me/CryptoBot)**
2. Запустите бота командой `/start`
3. Введите команду `/myapps` — откроется меню управления приложениями
4. Нажмите **"Create App"** (Создать приложение)
5. Введите название приложения (например, "My Exchange Bot")
6. Бот выдаст вам **API Token** — скопируйте его

**Формат токена:**
```
12345:AAAAAAA-BBBBBBB-CCCCCCC-DDDDDDD
```

⚠️ **ВАЖНО:** Никому не показывайте этот токен!

### Шаг 1.2: Проверка токена

Проверьте, что токен работает:
```bash
curl -X GET "https://pay.crypt.bot/api/getMe" \
  -H "Crypto-Pay-API-Token: ВАШ_ТОКЕН"
```

Должен вернуться JSON с информацией о приложении.

---

## 2. Настройка секретов в проекте

### Шаг 2.1: Добавление CRYPTO_BOT_API_TOKEN

1. Откройте проект в **poehali.dev**
2. В редакторе появится **виджет секретов** (справа вверху)
3. Найдите секрет `CRYPTO_BOT_API_TOKEN`
4. Нажмите **"Добавить"** и вставьте токен из Шага 1.1
5. Сохраните

### Шаг 2.2: Проверка других секретов

Убедитесь, что есть секреты:
- ✅ `DATABASE_URL` — автоматически создан
- ✅ `AWS_ACCESS_KEY_ID` — автоматически создан
- ✅ `AWS_SECRET_ACCESS_KEY` — автоматически создан
- ✅ `CRYPTO_BOT_API_TOKEN` — добавили в Шаге 2.1

---

## 3. Настройка Webhook

### Шаг 3.1: URL Webhook

Ваш webhook для Crypto Bot уже развернут:
```
https://functions.poehali.dev/da9602d2-ce86-4372-bbb4-5d1d7babe8ce
```

### Шаг 3.2: Регистрация Webhook в Crypto Bot

Выполните команду (замените `ВАШ_ТОКЕН`):

```bash
curl -X POST "https://pay.crypt.bot/api/setWebhook" \
  -H "Crypto-Pay-API-Token: ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "webhook_url": "https://functions.poehali.dev/da9602d2-ce86-4372-bbb4-5d1d7babe8ce"
  }'
```

**Ожидаемый ответ:**
```json
{
  "ok": true,
  "result": {
    "webhook_url": "https://functions.poehali.dev/da9602d2-ce86-4372-bbb4-5d1d7babe8ce"
  }
}
```

### Шаг 3.3: Проверка Webhook

```bash
curl -X GET "https://pay.crypt.bot/api/getWebhookInfo" \
  -H "Crypto-Pay-API-Token: ВАШ_ТОКЕН"
```

---

## 4. Создание Telegram бота

### Шаг 4.1: Создание бота через BotFather

1. Откройте **[@BotFather](https://t.me/BotFather)** в Telegram
2. Отправьте команду `/newbot`
3. Введите **название бота** (например, "My Crypto Exchange")
4. Введите **username бота** (например, `my_crypto_exchange_bot`)
5. BotFather выдаст **токен бота** — сохраните его

**Формат токена бота:**
```
1234567890:AAHHhhGGgg_FfDdCcBbAa-XxYyZz123456
```

### Шаг 4.2: Настройка описания бота

```
/setdescription @my_crypto_exchange_bot
```

Введите описание:
```
Быстрый обмен криптовалюты. Пополнение, вывод, обмен BTC, ETH, USDT ⚡️
```

### Шаг 4.3: Добавление изображения

```
/setuserpic @my_crypto_exchange_bot
```

Загрузите логотип (например, изображение с криптовалютами).

---

## 5. Подключение Web App к боту

### Шаг 5.1: Получение URL вашего приложения

1. В **poehali.dev** нажмите **"Опубликовать"**
2. Скопируйте URL (например, `https://ваш-проект.poehali.app`)

### Шаг 5.2: Создание Menu Button

Вернитесь в **[@BotFather](https://t.me/BotFather)**:

```
/mybots
→ Выберите вашего бота
→ Bot Settings
→ Menu Button
→ Configure menu button
```

**URL для Menu Button:**
```
https://ваш-проект.poehali.app
```

**Текст кнопки:**
```
🚀 Открыть Exchange
```

### Шаг 5.3: Альтернативный способ — через команду

Добавьте команду `/start` в боте:

```
/setcommands @my_crypto_exchange_bot
```

Введите:
```
start - Открыть криптообменник
```

Затем создайте скрипт бота (например, на Python):

```python
from telegram import Bot, WebAppInfo, InlineKeyboardMarkup, InlineKeyboardButton

bot = Bot(token="ВАШ_ТОКЕН_БОТА")

@bot.message_handler(commands=['start'])
def start(message):
    keyboard = InlineKeyboardMarkup([
        [InlineKeyboardButton(
            text="🚀 Открыть Exchange",
            web_app=WebAppInfo(url="https://ваш-проект.poehali.app")
        )]
    ])
    
    bot.send_message(
        chat_id=message.chat.id,
        text="Добро пожаловать в Crypto Exchange!\n\nНажмите кнопку ниже:",
        reply_markup=keyboard
    )
```

---

## 6. Тестирование

### Шаг 6.1: Регистрация тестового пользователя

1. Откройте вашего бота в Telegram
2. Нажмите `/start`
3. Откройте Web App
4. Приложение автоматически зарегистрирует пользователя в БД

### Шаг 6.2: Тест пополнения

1. На главной странице нажмите **"Пополнить"**
2. Выберите валюту (например, USDT)
3. Введите сумму (например, 10)
4. Нажмите **"Создать счет на оплату"**
5. Откроется ссылка на оплату в Crypto Bot
6. После оплаты webhook автоматически зачислит средства

### Шаг 6.3: Тест обмена

1. Перейдите на вкладку **"Обмен"**
2. Выберите валюту (например, USDT → BTC)
3. Введите сумму
4. Нажмите **"Обменять"**
5. Заявка создастся и появится в истории

### Шаг 6.4: Тест вывода

1. На главной нажмите **"Вывести"**
2. Выберите способ (карта или крипта)
3. Введите реквизиты
4. Нажмите **"Вывести средства"**

---

## 7. Частые проблемы

### ❌ Проблема: "Crypto Bot API error: 1010"

**Причина:** Неверный или не добавленный токен.

**Решение:**
1. Проверьте токен в [@CryptoBot](https://t.me/CryptoBot) командой `/myapps`
2. Убедитесь, что токен добавлен в секреты проекта
3. Перезапустите функции (sync_backend)

---

### ❌ Проблема: Webhook не получает уведомления

**Причина:** Webhook не зарегистрирован или URL неверный.

**Решение:**
```bash
# Проверка webhook
curl -X GET "https://pay.crypt.bot/api/getWebhookInfo" \
  -H "Crypto-Pay-API-Token: ВАШ_ТОКЕН"

# Переустановка webhook
curl -X POST "https://pay.crypt.bot/api/setWebhook" \
  -H "Crypto-Pay-API-Token: ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "webhook_url": "https://functions.poehali.dev/da9602d2-ce86-4372-bbb4-5d1d7babe8ce"
  }'
```

---

### ❌ Проблема: "User not found" после оплаты

**Причина:** Пользователь не зарегистрирован в БД.

**Решение:**
1. Откройте Web App в Telegram
2. Приложение автоматически зарегистрирует пользователя
3. Затем создайте счет на оплату

---

### ❌ Проблема: Кошельки пустые (balance = 0)

**Причина:** Не было пополнений.

**Решение:**
1. Создайте тестовый платеж через **"Пополнить"**
2. Оплатите счет в Crypto Bot
3. Webhook автоматически зачислит средства на кошелек

---

## 📊 Структура базы данных

Ваши таблицы:
- `users` — пользователи с реферальными кодами
- `wallets` — кошельки BTC, ETH, USDT, RUB
- `exchange_orders` — заявки на обмен
- `transactions` — история транзакций
- `exchange_rates` — курсы валют с наценкой
- `notifications` — уведомления для пользователей

---

## 🔗 Полезные ссылки

- **Crypto Bot API Docs:** https://help.crypt.bot/crypto-pay-api
- **Telegram Bot API:** https://core.telegram.org/bots/api
- **Telegram Web Apps:** https://core.telegram.org/bots/webapps

---

## 🎯 Важные URL проекта

### Backend Functions:
```
Auth:            https://functions.poehali.dev/0361ad2c-e0b4-4a43-9a5e-064e74824ed1
Wallets:         https://functions.poehali.dev/86bcee4a-dd62-495e-a557-c1c91198e7e4
Exchange:        https://functions.poehali.dev/3c872717-fdf0-4fb6-9d0c-2a3ab44b22bf
Rates:           https://functions.poehali.dev/68afa9d4-8dbf-4c81-960b-4a45c9e2cf92
Notifications:   https://functions.poehali.dev/dc7bc38a-fb4b-4c3c-be7e-d9b8cb0a48f2
Crypto Bot:      https://functions.poehali.dev/adee272e-5b6a-4a6a-a6d2-a599136ab133
Webhook:         https://functions.poehali.dev/da9602d2-ce86-4372-bbb4-5d1d7babe8ce
```

---

## ✅ Чеклист запуска

- [ ] Получил токен в [@CryptoBot](https://t.me/CryptoBot) через `/myapps`
- [ ] Добавил токен в секреты проекта (CRYPTO_BOT_API_TOKEN)
- [ ] Зарегистрировал webhook в Crypto Bot
- [ ] Создал Telegram бота через [@BotFather](https://t.me/BotFather)
- [ ] Настроил Menu Button с URL приложения
- [ ] Опубликовал проект на poehali.dev
- [ ] Протестировал пополнение → webhook → зачисление
- [ ] Протестировал обмен валют
- [ ] Протестировал вывод средств

---

## 🎉 Готово!

Теперь ваш криптообменник работает в Telegram!

**Поддержка:** [@poehali_dev](https://t.me/+QgiLIa1gFRY4Y2Iy)
